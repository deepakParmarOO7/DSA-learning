// find the majority element where any element that appears more then n/2

const findMajorityElement = (arr) => {
    mEle = -1
    let n = arr.length
    check = Math.floor(n/2)

    for(let i=0;i<n;i++){

    }
}

arr=[8,7,6,8,6,6,6,6]
const res = findMajorityElement(arr)
console.log(res);
